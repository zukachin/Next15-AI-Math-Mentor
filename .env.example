# Use "groq" (default) or "gemini"
LLM_PROVIDER=groq

# For Groq (free tier, fast)
GROQ_API_KEY=your_groq_api_key_here

# Optional: for Gemini (e.g. audio input when using Groq, or set LLM_PROVIDER=gemini)
GEMINI_API_KEY=your_gemini_api_key_here
